<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rahul.salescrm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SalesApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SalesCRM"
        android:usesCleartextTraffic="true">

        <activity
            android:name="com.rahul.activities.BottomBarActivity"
            android:exported="false" />
        <activity
            android:name="com.rahul.activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MySplashABCD">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>